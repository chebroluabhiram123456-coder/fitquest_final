name: fitquest_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase Core
  firebase_core: ^2.15.1
  
  # Firestore Database
  cloud_firestore: ^4.9.1
  
  # Firebase Authentication
  firebase_auth: ^4.9.0
  
  # Calendar for progress and history
  table_calendar: ^3.0.9
  
  # For formatting dates
  intl: ^0.18.1
  
  # State Management
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
